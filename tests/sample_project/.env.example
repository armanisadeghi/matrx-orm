# ============================================================
# matrx-orm Sample Project — Environment Variables
# ============================================================
#
# Copy this file to .env and fill in your values.
# The .env file is NEVER committed to git.
#
# ============================================================
# Output paths  (REQUIRED — do not put these in matrx_orm.yaml)
# ============================================================
#
# MATRX_PYTHON_ROOT is required. Set it to the root of your Python project.
# Generated Python files will land at:  $MATRX_PYTHON_ROOT/database/{alias}/
#
MATRX_PYTHON_ROOT=/absolute/path/to/your/python/project

# MATRX_TS_ROOT is only required when output.typescript: true in matrx_orm.yaml.
# Set it to the root of your Next.js project.
# Generated TypeScript files will land in subdirectories under $MATRX_TS_ROOT.
# Leave commented out (or omit) when typescript: false.
#
# MATRX_TS_ROOT=/absolute/path/to/your/nextjs/project

# MATRX_SAVE_DIRECT overrides save_direct in matrx_orm.yaml.
# true  = write directly to the roots above (git must be clean first).
# false = write to a temp staging directory for review.
#
# MATRX_SAVE_DIRECT=true

# ============================================================
# Primary database  (e.g. your main Supabase project)
# ============================================================
PRIMARY_DB_HOST=enter_your_primary_db_host_here
PRIMARY_DB_PORT=5432
PRIMARY_DB_PROTOCOL=postgresql
PRIMARY_DB_NAME=postgres
PRIMARY_DB_USER=postgres
PRIMARY_DB_PASSWORD=your-database-password-here

# ============================================================
# Secondary database  (e.g. a second Supabase project or remote Postgres)
# ============================================================
SECONDARY_DB_HOST=enter_your_secondary_db_host_here
SECONDARY_DB_PORT=5432
SECONDARY_DB_PROTOCOL=postgresql
SECONDARY_DB_NAME=postgres
SECONDARY_DB_USER=postgres
SECONDARY_DB_PASSWORD=your-database-password-here
